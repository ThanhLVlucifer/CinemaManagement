package com.example.cinemamanagement.controller.trailer;

public class TrailerPresenter {
    private final TrailerView trailerView;

    public TrailerPresenter(TrailerView trailerView) {
        this.trailerView = trailerView;
    }
}
